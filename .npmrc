# Bun is the primary package manager for this project.
# This .npmrc is retained for Node.js / npm / pnpm compatibility.

# Use node-modules linker for better compatibility
node-linker=hoisted

# Strict peer dependencies
strict-peer-dependencies=false

# Auto-install peers (recommended for easier setup)
auto-install-peers=true

# Use semantic versioning for flexibility
save-exact=false
