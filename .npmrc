# Use node-modules linker for better compatibility
node-linker=hoisted

# Strict peer dependencies
strict-peer-dependencies=false

# Auto-install peers (recommended for easier setup)
auto-install-peers=true

# Save exact versions (optional but recommended)
save-exact=false
